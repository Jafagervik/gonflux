@import "std"


main :: () -> 

    a: Array[i32] = [1,2,3,4,5]

    // For 
    for elem in a ->
        print(elem)
    end

    // For in range
    for i in 0..=10 ->
        
    end 

    // For with index
    for |idx, elem| in elem.enumerate() ->
        
    end


    // While 
    while (a) || ->

    end

end
